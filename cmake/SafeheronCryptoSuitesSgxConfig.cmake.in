@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/SafeheronCryptoSuitesSgxTargets.cmake")

message(STATUS "SafeheronCryptoSuitesSgx: Loading package in compatibility mode.")
set_and_check(SafeheronCryptoSuitesSgx_INCLUDE_DIR "${PACKAGE_PREFIX_DIR}/include")
set_and_check(SafeheronCryptoSuitesSgx_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include")
set_and_check(SafeheronCryptoSuitesSgx_LIBRARY_DIR "${PACKAGE_PREFIX_DIR}/lib")
get_target_property(SafeheronCryptoSuitesSgx_LIBRARY
                    SafeheronCryptoSuitesSgx
                    IMPORTED_LOCATION)
if(NOT SafeheronCryptoSuitesSgx_LIBRARY)
    get_target_property(SafeheronCryptoSuitesSgx_LIBRARY
                        SafeheronCryptoSuitesSgx
                        IMPORTED_LOCATION_RELEASE)
endif()

if(NOT SafeheronCryptoSuitesSgx_LIBRARY)
    message(FATAL_ERROR "Could not determine library location for target SafeheronCryptoSuitesSgx. The installation may be broken.")
else()
    set_and_check(SafeheronCryptoSuitesSgx_LIBRARY "${SafeheronCryptoSuitesSgx_LIBRARY}")
    message(STATUS "Found SafeheronCryptoSuitesSgx: ${SafeheronCryptoSuitesSgx_LIBRARY}")
endif()